This folder includes scripts that can be run in case something out of the ordinary happens and you must manually recover.


Database:
- `npm run database-Reset` - Interactive prompt to help reset the version